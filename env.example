# LiveSpeak Environment Variables
# Google Cloud API Configuration

# Google Cloud Project ID (Required)
# Get this from your Google Cloud Console project settings
GOOGLE_CLOUD_PROJECT_ID=your_project_id_here

# Google Cloud Service Account Key File Path (Required)
# This should point to your JSON service account key file
# Example: GOOGLE_APPLICATION_CREDENTIALS=./google-credentials.json
GOOGLE_APPLICATION_CREDENTIALS=./google-credentials.json

# Alternative: Direct API Key (if not using service account)
# GOOGLE_CLOUD_API_KEY=your_api_key_here

# Environment
NODE_ENV=development

# API Configuration
GOOGLE_SPEECH_API_URL=https://speech.googleapis.com/v1/speech:recognize
GOOGLE_TRANSLATE_API_URL=https://translation.googleapis.com/language/translate/v2

# Rate Limiting (Optional)
MAX_REQUESTS_PER_MINUTE=60
MAX_REQUESTS_PER_DAY=1000

# Logging (Optional)
LOG_LEVEL=info
ENABLE_API_LOGGING=true

# Note: 
# 1. Copy this file to .env and fill in your actual values
# 2. Never commit .env to git
# 3. For production, use environment variables or secure secret management
